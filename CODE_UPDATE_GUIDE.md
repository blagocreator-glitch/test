# Руководство по обновлению ID в коде

## 🔄 Автоматическая замена (простые случаи)

Для автоматической замены простых ID (1:1) выполните:

```bash
node auto_replace_simple_ids.js
```

Это обновит 1121 простых замен в следующих файлах:
- prices_list.js
- calc-core.js
- calc-utils.js
- demolition.js
- calc-init.js
- calc-render.js
- calc-repair-quest.js
- calc-building.js
- calc-pricing.js
- calc-flow.js

## ⚠️ Ручная замена (контекстные случаи)

Следующие 105 ID требуют ручного анализа контекста:


### `floor_linoleum`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_linoleum'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_ceramic`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_ceramic'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_porcelain`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_porcelain'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_quartzvinyl`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_quartzvinyl'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_laminate`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_laminate'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_carpet`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_carpet'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_engineered`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_engineered'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_solid`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_solid'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_parquet`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_parquet'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_cork`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_cork'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `wall_wallpaper`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'wall_wallpaper'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `wall_paint`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'wall_paint'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `wall_decorative_plaster`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'wall_decorative_plaster'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `wall_microcement`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'wall_microcement'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `wall_ceramic`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'wall_ceramic'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `wall_porcelain`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'wall_porcelain'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `wall_mosaic`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'wall_mosaic'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `ceiling_suspended`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'ceiling_suspended'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `ceiling_stretch`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'ceiling_stretch'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `ceiling_ree`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'ceiling_ree'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `ceiling_gk`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'ceiling_gk'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `ceiling_cassette`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'ceiling_cassette'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `ceiling_grilyato`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'ceiling_grilyato'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `ceiling_paint`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'ceiling_paint'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `ceiling_waterproof`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'ceiling_waterproof'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_level_csp_5cm`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_level_csp_5cm'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_level_csp_over_5cm`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_level_csp_over_5cm'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_level_halfs`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_level_halfs'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_level_dry_knauf`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_level_dry_knauf'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_level_self`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_level_self'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_level_poly`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_level_poly'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_waterproof_membrane`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_waterproof_membrane'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_level_warm_prep`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_level_warm_prep'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_level_waterproof`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_level_waterproof'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_level_sound`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_level_sound'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `ceiling_vapor_barrier`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'ceiling_vapor_barrier'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `boiler_connection`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'boiler_connection'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `thermostat_heating`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'thermostat_heating'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_heating_water`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_heating_water'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_heating_collector`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_heating_collector'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `floor_heating_electric`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'floor_heating_electric'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `wall_base_primer`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'wall_base_primer'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `wall_base_putty_local`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'wall_base_putty_local'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `wall_putty_finish_paint`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'wall_putty_finish_paint'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `wall_paint_primer_deep`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'wall_paint_primer_deep'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `wall_paint_sanding`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'wall_paint_sanding'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `wall_paint_remove_old`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'wall_paint_remove_old'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `wall_baseboard_panel_joint`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'wall_baseboard_panel_joint'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `ceiling_base_primer`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'ceiling_base_primer'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `ceiling_base_putty_local`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'ceiling_base_putty_local'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `ceiling_putty_finish`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'ceiling_putty_finish'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `ceiling_glass_fiber`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'ceiling_glass_fiber'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `ceiling_paint_sanding`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'ceiling_paint_sanding'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_concrete_straight_h3_w11`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_concrete_straight_h3_w11'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_concrete_straight_h6_w15`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_concrete_straight_h6_w15'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_concrete_l_h3_w11`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_concrete_l_h3_w11'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_concrete_l_h6_w15`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_concrete_l_h6_w15'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_concrete_u_h3_w11`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_concrete_u_h3_w11'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_concrete_u_h6_w15`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_concrete_u_h6_w15'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_concrete_spiral_d2_h3`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_concrete_spiral_d2_h3'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_concrete_spiral_d3_h6`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_concrete_spiral_d3_h6'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_concrete_straight`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_concrete_straight'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_concrete_l_shape`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_concrete_l_shape'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_concrete_u_shape`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_concrete_u_shape'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_concrete_spiral`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_concrete_spiral'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_metal_straight_h3_w11`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_metal_straight_h3_w11'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_metal_straight_h6_w15`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_metal_straight_h6_w15'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_metal_l_h3_w11`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_metal_l_h3_w11'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_metal_l_h6_w15`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_metal_l_h6_w15'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_metal_u_h3_w11`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_metal_u_h3_w11'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_metal_u_h6_w15`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_metal_u_h6_w15'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_metal_spiral_d2_h3`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_metal_spiral_d2_h3'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_metal_spiral_d3_h6`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_metal_spiral_d3_h6'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_metal_straight`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_metal_straight'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_metal_l_shape`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_metal_l_shape'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_metal_u_shape`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_metal_u_shape'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_metal_spiral`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_metal_spiral'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_wood_straight_h3_w11`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_wood_straight_h3_w11'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_wood_straight_h6_w15`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_wood_straight_h6_w15'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_wood_on_metal_h3_w11`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_wood_on_metal_h3_w11'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_wood_on_metal_h6_w15`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_wood_on_metal_h6_w15'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_wood_hardwood_h3_w11`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_wood_hardwood_h3_w11'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_wood_hardwood_h6_w15`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_wood_hardwood_h6_w15'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_wood_spiral_d2_h3`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_wood_spiral_d2_h3'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_wood_spiral_d3_h6`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_wood_spiral_d3_h6'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_wood_straight`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_wood_straight'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_wood_on_metal`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_wood_on_metal'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_wood_hardwood`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_wood_hardwood'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_wood_spiral`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_wood_spiral'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_stone_15steps_w11`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_stone_15steps_w11'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_stone_25steps_w15`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_stone_25steps_w15'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_granite_15steps_w11`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_granite_15steps_w11'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_granite_25steps_w15`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_granite_25steps_w15'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_marble_15steps_w11`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_marble_15steps_w11'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_marble_25steps_w15`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_marble_25steps_w15'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_stone`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_stone'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_granite`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_granite'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_marble`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_marble'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_composite_dpk_h15_w12`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_composite_dpk_h15_w12'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_composite_dpk_h3_w15`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_composite_dpk_h3_w15'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_composite_outdoor_h15_w12`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_composite_outdoor_h15_w12'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_composite_outdoor_h3_w15`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_composite_outdoor_h3_w15'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_composite_dpk`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_composite_dpk'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_composite_on_metal`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_composite_on_metal'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID

---

### `stair_install_composite_outdoor`

Этот ID используется в нескольких контекстах. Выберите правильную замену:



**Как определить правильный контекст:**
1. Найдите все использования `'stair_install_composite_outdoor'` в коде
2. Определите, в каком разделе используется ID:
   - Демонтаж → используйте ID с префиксом `demo_`
   - Черновые работы → используйте ID с префиксом `rough_`
   - Инженерные работы → используйте ID с префиксом `eng_`
   - Чистовые работы → используйте ID с префиксом `finish_`
3. Замените на соответствующий новый ID


## 📋 Чек-лист после обновления

- [ ] Выполнена автоматическая замена простых ID
- [ ] Выполнена ручная замена контекстных ID
- [ ] Проверены все файлы калькулятора
- [ ] Запущены тесты (если есть)
- [ ] Проверена работа калькулятора в браузере
- [ ] Проверена корректность расчёта цен
- [ ] Проверена работа демонтажа
- [ ] Проверена работа монтажа
- [ ] Создан коммит с изменениями

## 🔍 Поиск использований ID

Для поиска всех использований конкретного ID:

```bash
# Windows
findstr /s /i "old_id_name" *.js

# Unix/Linux/Mac
grep -r "old_id_name" *.js
```

## 📊 Статистика

- Всего ID для замены: 1226
- Простых замен: 1121
- Контекстных замен: 105

## 🔗 Полезные файлы

- `price_list_id_mapping.json` — Полный маппинг всех ID
- `code_update_recommendations.json` — Детальные рекомендации
- `PRICE_LIST_NEW_STRUCTURE.md` — Документация по новой структуре
